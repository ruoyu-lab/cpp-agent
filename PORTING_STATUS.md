# Native C++ Porting Status

The NodeJS project remains the working TypeScript implementation in `NodeJS`. The C++ project is a parallel native version for runtime-constrained business scenarios, not a replacement for NodeJS.

| NodeJS area | C++ module(s) | Status | Notes / remaining work |
|---|---|---:|---|
| `core` | `core`, `messages`, `execution`, `hooks`, `http` | Partial | Errors, `Value`, JSON parsing, schema validation including Node-style top-level object schema normalization and typed `additionalProperties`, schema Value serialization helpers, Node-style message/content/tool-call normalization, finish reasons, cancellation with active callback registration, timeout policy enforcement including Node-style permission timeout omission, Node-style retry delay resolution including `none`/fixed/exponential caps and jitter ranges, events including constructor-configured sinks, `includeRaw`, Node-style sink error isolation, and Node-style no-implicit-trace publishing, Node-style trace context creation/child derivation/normalization helpers, hook sets/merging, injected HTTP JSON/stream helpers, zero-dependency native HTTP/1.1 client transport for plain HTTP, line parsing, and SSE data parsing are ported. TLS/HTTPS remains available only through injected transports under the zero-dependency constraint. |
| `model` | `model` | Partial | Chat/text/image embedding interfaces, echo model, fallback chat model adapter chains, default stream event fallback, reasoning settings validation/merge, effort/token budget normalization, non-stream and stream tagged reasoning extraction, hash text/image embeddings, injected CLIP text/image embedding adapters with shared default space id, embedding cancellation propagation, config-created injected llama.cpp-native chat/text embedding adapters with tool-envelope and structured-output grammar request support, native OpenAI/Qwen/Ollama/Gemini text embedding adapters, synchronized provider registries with execution-time factory snapshots, OpenAI-compatible chat and Responses API serialization/parsing, callback transport with request cancellation propagation, zero-dependency default HTTP provider transport for plain HTTP, provider-native chat and Responses stream parsing through injected stream transports, and config-created provider transports are ported. TLS/HTTPS provider calls require an injected transport; full upstream provider protocol edge cases remain. |
| `provider-openai`, `provider-qwen`, `provider-gemini`, `provider-anthropic`, `provider-deepseek`, `provider-ollama`, `provider-llamacpp-native` | `model`, `http`, `config` | Partial | OpenAI-compatible plus Qwen, Anthropic, DeepSeek, Ollama, and Gemini request builders, auth/header shaping including OpenAI organization headers, native and tagged reasoning/tool-call parsing for non-stream and stream responses with Node-style non-object argument wrapping, OpenAI/Qwen Responses API routing for reasoning/non-text inputs including Qwen `reasoningApi`, Qwen/Anthropic/Gemini reasoning budget mapping, Node-style config default model/env resolution for OpenAI/Qwen/Gemini/Anthropic/DeepSeek/Ollama, provider transport cancellation propagation for chat and embeddings, llama.cpp-native runtime binding surface plus optional built-in `libllama`/`libggml` dynamic loader with model cache, chat sampling, streaming deltas, session KV reuse, LoRA, grammar sampling, text embeddings, cancellation, and `libmtmd`/`mmproj` image input path, tool-history serialization, tool-call JSON envelope parsing, structured-output JSON Schema validation and GBNF generation, session/grammar/Lora config passthrough, active chat/embedding request cancellation forwarding to native bindings, tagged reasoning streaming, embedding dimension validation, config-created chat/embedding adapters, and `modelPathEnv`/`libraryPathEnv`/`libraryDirEnv`/`mmprojPathEnv`/`mtmdLibraryPathEnv`/`mtmdLibraryDirEnv` reference collection, native callback adapters, config adapter selection, reusable zero-dependency plain-HTTP socket transport, HTTP provider transport bridge, and provider-native chat stream parsing through injected stream transports are ported. TLS/HTTPS/provider calls require injected transports under the zero-dependency constraint; built-in llama.cpp runtime execution requires enabling `AGENT_NATIVE_ENABLE_LLAMA_CPP` and developer-provided llama.cpp headers/libraries; full upstream provider protocol edge cases remain. |
| `tools`, `tools-builtin-core`, `tools-builtin-data`, `tools-builtin-local`, `tools-builtin-http`, `tools-builtin-web`, `tools-builtin-browser`, `tools-builtin-repository`, `tools-builtin-agent`, `tools-builtin-workflow`, `tools-builtin-developer` | `tools`, `builtins`, `http`, `browser`, `web`, `memory`, `workflow`, `server` | Partial | Tool definitions, synchronized registry with execution-time tool snapshots, synchronized bundle registry with creation-time provider snapshots, permission policy builders, static/CLI/memory/manual approval handlers, approval stores, executor, Node-style result-envelope message metadata, Node-style tool failure and permission-denial wrapping with raw causes/reasons preserved in outputs/events, `tool.audit` lifecycle events, `permission.checked` allow events, executor-created tool trace contexts, skill `allowed-tools` preapproval, typed plus JSON tool execution services, core tools including Node-style `random.number` inclusive-min/exclusive-max behavior with invalid-range rejection, zero-dependency `http.fetch`, and `json.query` object/array path selection, data/local tools, injected-transport HTTP tools including Node-style strict JSON parsing for `http.inspectJson`, injected process developer tools, browser render/extract/screenshot tools, web search/fetch/crawl tools including default `web.fetch` support for file/plain-HTTP URLs, Node-style cancellation propagation into HTTP/developer/browser/web builtin adapter requests, repository read tools, session snapshot, KnowledgeBase/KnowledgeBaseManager search tools including explicit `topK: 0`, workflow get/resume/checkpoint tools, builtin bundle metadata registry, bundle-level tag/capability merging, and builtin context-service fallback are ported. Broader NodeJS package edge-case parity remains. |
| `memory` | `memory`, `server` | Mostly ported | Session memory with Node-style chainable add/addMany/restore, `MessageInput` Value add/addMany, getMessages options, partial restore, custom summarizers, and store-level session options, synchronized in-memory/file session stores with config-object construction, Node-style encoded file names, immediate file-backed on-change persistence, and session id listing, synchronized in-memory/file vector stores including config-object construction, Node-style store namespace fallback for upserts, explicit `topK: 0`, C++ `delete_ids` alias for NodeJS `delete`, erase/clear/count, long-term memory including Node-style config construction, object-style `remember`, object-style search/context options, explicit `topK: 0`, `rememberMany`, `buildContextMessage`, Node-style nested plan conversation writeback, record-returning conversation writeback, Node-style JSON serialization helpers for records/hits/context results shared by runtime hooks, durable checkpoints, and server run summaries, auto writeback, and core server session inspection/deletion routes are ported. External stores remain. |
| `knowledge` | `memory` | Partial | Documents, chunking, text/file/directory/markdown/repository/GitHub/web/website/sitemap loader surfaces including Node-style website `maxPages: 0` and sitemap `limit: 0`, synchronized loader/chunker/reranker/text-index/vector-index provider registries, GitHub ref URL encoding, native PDF text extraction, browser-rendered website ingestion, synchronized in-memory/file-backed knowledge stores, synchronized native memory/minisearch text indexes, synchronized namespace-aware in-memory/file-backed vector indexes, Qdrant/client-backed pgvector vector indexes including explicit `topK: 0` and serialized readiness initialization, synchronized KnowledgeBase ingest/search/delete/index-readiness paths, synchronized KnowledgeBaseManager manifest/create/load/delete/search lifecycle, text/image cross-modal ingest/search, direct ingest dedupe/skip-unchanged semantics, ingestion cancellation propagation with embedding batch control, Node-style per-base searchDefaults/retrievalConfig merging and production preset defaults, vector/lexical hybrid search with vectorTopK/lexicalTopK, retrievalMode, oversampleFactor, RRF/weighted fusion, modalityWeights, searchWithDebug candidate breakdowns, buildContextMessage-style hit/message/debug results, knowledgeBaseTitle citations, configurable contextTitle, knowledgeBaseId metadata, and full search-hit record serialization including timestamps and embeddings, heuristic/basic/overlap/hybrid/recency/MMR rerankers including explicit `topK: 0`, ingestion pipeline with pluggable dedupe/incremental/retry strategies, synchronized in-memory/file sync state stores, config-triggered sync ingestion with default production sync state paths and delete-missing sync jobs, config-created knowledge bases/managers, context messages, and search-hit JSON serialization are ported. Native pgvector connection factory remains injected and zero-dependency. |
| `runtime` | `context`, `runtime` | Partial | Synchronized embedded context registry, planning render, execution-plan JSON normalization/parsing/serialization, Planner interface, StaticPlanner, ModelPlanner, config-created static/model planners, runner plan injection, Node-style per-call run/stream planning disable, Node-style stream `plan` event naming and no empty plan event when planners return null, run-result plan reporting, automatic KnowledgeBase/KnowledgeBaseManager context retrieval including Node-style image-only `MessageInput` knowledge queries, Node-style long-term memory retrieval/writeback tri-state defaults with per-call merge, namespace writeback, and plan-step summaries, memory/knowledge hit propagation into planner/runtime context/results, AgentLoop and AgentRunner durable checkpoint/resume state with serializable checkpoints, AgentRunner/AgentLoop multimodal input-part entrypoints for run/stream, Node-style full `AgentMessage` input entrypoints preserving role, message metadata, content-part metadata, and non-string slash-skill input content, CancellationToken propagation through runner/loop/model/retrieval/tool execution including model `GenerateParams` and retrieval embedding calls, timeout policy enforcement, Node-style retry delay scheduling and `retry.scheduled` EventBus events for model/retrieval/tool execution policies, stream retry before first emitted event and no retry after emitted stream output, stream external `CancellationToken` abort propagation, stream tool failures surfaced as failed `tool-complete` results with continued recovery, stream synthetic failed `tool-complete` recovery when the executor itself throws unexpectedly, Node-style aggregate stream `tools` events after each tool batch with replay/eval serialization, Node-style runner stream no-op `knowledge-retrieval`/`memory-retrieval` events, stream `incomplete-response` and `max-iterations` termination results, stream error event snapshots before rethrowing and `model.failed` events, stream status events for knowledge/memory/planning/model/tool stages, stream knowledge/planning events, agent loop, runner, synchronous stream-event execution for model deltas/tool progress/done results, run/model/tool/permission/retrieval lifecycle hook dispatch including runner stream `before_run`/`after_run`/`on_run_error`, Node-style run/stream root trace creation plus model/tool child trace propagation, run started/completed/failed EventBus events, retrieval started/completed/failed events, run context propagation into embedded contexts/tool services, merged configured/context/runtime tool services with typed references including runner-bound knowledge services, retrieval/writeback options, skill catalog/activation prefaces, slash-skill effective input, selected/default/auto-selected skills, fork-skill callback and named subagent execution, fork result service propagation, and skill service propagation are ported. Broader async server cancellation edge cases and richer lifecycle callbacks remain. |
| `workflow` | `workflow`, `server` | Partial | Builder helpers including Node-style `create_workflow`/`create_node` and options-object builder overloads, synchronized node/agent registries with execution-time handler snapshots, default start/transform/tool/agent/condition/human-wait/artifact/router/join/webhook-wait/end handlers, direct `workflow-node-human` response-provider and `workflow-node-webhook` injected-transport handler factories, workflow state/checkpoint/signal serialization, synchronized in-memory/file stores including Node-style config-object construction and `encodeURIComponent` run filenames, Node-style workflow engine config-object construction, Node-style object inputs for run/resume/human/webhook signal APIs, workflow event-bus publishing, node execution retry policies with retry scheduled events, persisted resume, human/webhook submit APIs, workflow/node/child-agent lifecycle hook dispatch, child-agent callback registry, direct AgentRunner child-agent binding with workflow context propagation, custom node handlers, config-created workflow engines/stores, workflow inspection tools, workflow tool execution services, and core server workflow routes are ported. Richer async/concurrency behavior and broader async/concurrency edge-case parity remain. |
| `tasks`, `tasks-bullmq`, `tasks-postgres` | `tasks` | Partial | In-memory/file task store with Node-style config-object construction for file stores, in-memory queues, and workers, synchronized queue/store mutations, leases, runs, events, checkpoints, worker, active worker cancellation token propagation, BullMQ-compatible push queue/worker semantics with synchronized injected queue-client lifecycle, and Postgres task-store SQL semantics with synchronized readiness/close lifecycle through an injected zero-dependency query client are ported. Production BullMQ/Postgres client implementations remain external/injected under the zero-dependency constraint. |
| `realtime`, `realtime-redis` | `execution` | Partial | In-memory pub/sub with Node-style config-object construction, replay buffer, Node-style subscribe replay options for explicit counts and `replay: true`, subscription-object unsubscribe overloads, EventBus realtime sink with static or event-mapped topic/metadata, Value-sequence realtime pipe helper, and Redis-compatible pub/sub semantics through an injected zero-dependency client interface are ported. Production Redis network client implementations remain external/injected under the zero-dependency constraint. |
| `orchestration` | `orchestration` | Mostly ported | Synchronized in-memory artifacts/shared state/mailbox, synchronized file artifact/shared-state stores with Node-style config-object construction, encoded namespace filenames, shared-state array persistence, and legacy native file-shape reads, artifacts including Node-style `clear-all` history records, routing, mailbox task routing, plan-act-observe coordinator, rule/artifact-aware replanning strategies, evaluator loop coordinator, supervisor-worker coordinator, supervisor-evaluator coordinator, workflow-backed coordinator bridge, supervisor-workflow coordinator, and threshold/keyword evaluation strategies are ported. Production persistence edge-case parity remains. |
| `media` | `messages`, `media` | Partial | Inline/path/file URL/data URL/artifact resolution, MIME inference, zero-dependency native plain-HTTP media fetch resolver for document preprocessing, synchronized document preprocessor/OCR/rasterizer registries with lookup snapshots, default file/image/text/PDF preprocessor flow, native PDF text extraction, OCR provider registry integration, and document rasterizer registry integration are ported. TLS/HTTPS media fetch requires an injected resolver/transport; broader document extraction and production OCR/rasterizer providers remain. |
| `web`, `web-crawler`, `web-search-*` | `web` | Partial | Synchronized static search registry with execution-time provider snapshots, host-aware domain filtering, Brave/SearXNG providers with Node-style query parameters, API-key/default-env validation, and post-filter topK handling including explicit `topK: 0`, synchronized registered/file/injected/default page fetcher state, zero-dependency native plain-HTTP search and page fetch transports, Node-style default timeout and extract-mode field semantics, search/fetch/crawl cancellation token propagation, browser-backed fetch fallback, page value serialization, native bounded crawler with synchronized fetcher pointer access, Node-style default page limits including explicit `maxPages: 0`, result shape, page failure propagation, sitemap discovery, robots and allowed-domain block reporting, absolute crawl link output, crawler profile defaults, and web config wiring are ported. TLS/HTTPS web transport requires injection; richer concurrency controls and full upstream search-provider edge cases remain. |
| `skills` | `skills` | Mostly ported | Anthropic-style skill markdown parsing, typed frontmatter metadata preservation, Node-style argument/env prompt rendering, filesystem `SKILL.md` loading, project/user registry discovery, synchronized skill registry access, deterministic automatic selection, runtime resolution, runner activation, fork/subagent bridge hooks, and config wiring are ported. Remaining work is limited to broader integration edge cases outside the current NodeJS `packages/skills` surface. |
| `mcp` | `mcp` | Partial | In-process MCP tools/resources/prompts including synchronized resource/prompt snapshots, multi-content resource reads, prompt argument metadata, tool summary and structured-value rendering helpers, tool error content/_meta preservation, default object input schema for schema-less remote tools, JSON-RPC helpers with string/numeric id normalization, synchronous client with Node-style pending response matching for delayed injected transports, line-based stdio stream transport, POSIX stdio subprocess transport with start/send/response-wait/close lifecycle, callback/HTTP-injected transports, native plain-HTTP JSON-RPC POST transport for URL-based `.mcp.json` servers, Anthropic `.mcp.json` parsing/loading/discovery, config path/env/cwd resolution, default stdio/http server wiring plus `NativeMCPTransportFactory`, and MCP tool/resource/prompt adapters are ported. TLS/SSE/WebSocket transports and richer background server-message handling remain. |
| `replay` | `replay_eval`, `server` | Partial | HTML replay rendering, materialization, Node-style timestamped replay write options/result paths, optional-result manifest serialization/loading, manifest/HTML-or-directory replay loading, Node-style and native stream status/tool-call summaries, session run listing, runner stream status serialization, and server chat/stream replay writing with response/audit integration are ported. Full replay server/fallback behavior remains. |
| `evals`, `usage` | `model`, `replay_eval`, `cli` | Partial | Usage/cost extraction including Node-style raw response unwrapping, Gemini metadata field names, explicit zero-valued total token preservation, usage merge/provider retention, per-case model settings, Node-style string or `MessageInput` eval case inputs, basic and programmatic custom eval assertions, JavaScript-style regex output assertions, JSON Schema structured-output assertions, serializable built-in/custom assertion descriptors, tool/cost/token/latency/retrieval/citation/status-stage/permission assertion helpers, stream-event eval execution including knowledge-retrieval stages, permission event capture, CancellationToken propagation through eval case selection, runner streaming, assertion execution, and Node-style replay HTML path writes, JSON suite/report serialization, Node-style `define_eval_suite`, `load_eval_baseline`, object-style `run_eval_suite` options for runner-backed suites, per-case `create_runner` factories, and config-backed runner resolution from loaded configs, in-memory native configs, or config paths with injected provider/MCP/web/developer/browser/llama adapters while preserving resolved app lifetimes, richer report fields for usage/tool calls/status stages/permission events/tags/metadata/replay paths, Markdown reports, baseline comparison, replay-per-case integration, and native CLI eval execution/report writing are ported. Richer hosted eval runner orchestration remains. |
| `config` | `config` | Partial | Native config discovery follows the NodeJS default filename order (`.mjs`, `.js`, `.cjs`, `.json`, `.ts`) when walking upward from a directory, and JS/TS config modules can be loaded through an injected host-runtime `NativeConfigModuleLoader` from direct app loading, server config paths, and programmatic CLI runs instead of silently falling through to lower-priority JSON. Node-style `defineAgentConfig` and `LoadedAgentConfig` parity is exposed as `define_native_agent_config`, `NativeLoadedAgentConfig`, `define_native_loaded_agent_config`, `load_native_loaded_agent_config`, loaded-config app resolution, loaded-config `resolve_agent_id`/`resolve_agent_definition`, validation, and env-key collection/assertion with explicit cwd/path relative-path semantics. Native JSON config loading can validate agent/model/provider basics, model reasoning schema and provider capability gates, model fallback chains and required-capability negotiation, bundle names, resource refs, planner configs, knowledge retrieval options, knowledge store/text-index/vector-index/chunking/ingest option shapes and ranges including sqlite vector dimension/embedder consistency, knowledge embedder/image embedder providers including injected llama.cpp-native bindings, web browser fallback constraints, collect/assert referenced env keys for model/planner/web/knowledge configs and referenced resources including llama.cpp-native model/library env refs, and then resolve agent id, model, config-created text/image knowledge embedders, Node-style knowledge production preset defaults for chunking/textIndex/vectorIndex/searchDefaults/sync, config-created static/model planners, builtin bundles, session store, long-term memory, model settings, memory retrieval/writeback, knowledge retrieval, runner-bound knowledge bases/managers with resource refs, textIndex/vectorIndex selection, source ingestion with document/embedding batch options plus sync/deleteMissing handling, workflow engines/stores with resource refs, web search/fetch/crawler runtime and browser fallback through injected transports/adapters, browser renderer runtime through injected adapters, developer process execution through injected adapters, permission policies/approval handlers with resource refs, Anthropic-style skills/defaultSkills/advertiseSkills, and Anthropic MCP server configs/tools through an injected transport factory. Richer schema validation, built-in JS/TS config module execution, and richer resource backends remain. |
| `cli` | `cli`, `config`, `runtime`, `replay_eval` | Partial | Native `native_agent_cli` target, reusable CLI runner, help/version, config validation, config-backed prompt chat including programmatic host-loaded JS/TS module configs, manual provider/model chat with system prompts, persistent sessions, local/URL knowledge sources and retrieval debug output, project/user Anthropic skills, Anthropic `.mcp.json` tool loading, environment-driven Brave/SearXNG web search provider discovery, streaming terminal output with `--no-stream` fallback, interactive REPL with `:exit`/`:quit`/`:clear`/`:debug`, eval suite execution with report/baseline handling, and replay list/show commands are ported. Broader manual-mode embedding/provider edge-case parity remains. |
| `server`, `server-express` | `server`, `http`, `runtime`, `memory`, `tasks`, `autonomous`, `workflow`, `replay_eval`, `config` | Mostly ported | Transport-independent server app foundation is ported with synchronized route registration/snapshot matching, JSON body parsing, JSON/SSE response helpers, CORS/preflight behavior, bearer/API-key auth, API-key agent allowlists, API-key quota headers, fixed-runner, createRunner-style, and config-backed cached per-agent runner/workflow sources, task/autonomous owner and tenant access scopes, rate limiting, route/agent/API-key metrics snapshots including usage tokens and pricing-based estimated cost, request/trace ID normalization and response headers, execution-context trace propagation, JSONL audit log persistence for request failures/rejections and runtime mutations, request modelSettings reasoning validation, PII redaction, output-schema validation, citation policy summaries/enforcement, server replay writing, health/ready/whoami/metrics routes, AgentRunner-backed chat/stream routes, session list/get/delete routes with policy enforcement, approval list/resolve routes with manual approval mode and injected-Postgres approval store SQL semantics plus synchronized readiness/close lifecycle, task create/list/get/events/checkpoints/state/resume/cancel/delete-as-cancel routes with Node-style idempotency header/body resolution, require-key policy, session policy, and modelSettings reasoning validation, autonomous create/list/get/resume/cancel/waiting-step-complete routes with Node-style create/lifecycle statuses, strict optional-field validation, and injected-Postgres autonomous store SQL semantics, and workflow run/get/checkpoints/resume/human-response/webhook routes. HTTP listener/transport is the host's responsibility (NodeJS/Java/PHP/Go/…) — the native port intentionally ships no listener. |
| `browser`, `browser-playwright` | `browser`, `builtins`, `config`, `web`, `memory` | Partial foundation | Browser render request/result metadata, renderer interface, callback-backed renderer adapter, render/extract/screenshot builtin tools, browser bundle routing, config adapter wiring, web fetch browser fallback, and knowledge website render fallback are ported. Native Playwright/browser backend, process lifecycle, and screenshots from a real engine remain. |
| `ocr`, `ocr-tesseract`, `document-rasterizer`, `document-rasterizer-pdfjs`, `embedding-clip` | `media`, `model` | Partial foundation | OCR provider interfaces/registry and document rasterizer interfaces/registry are ported and wired into media preprocessing. CLIP text/image embedding adapter surfaces are ported with injected batch execution under the zero-dependency runtime. Native Tesseract/PDF rasterizer providers and bundled CLIP/Transformers model execution remain external/injected. |
| `tool-client`, realtime socket adapter | `tool_client` | Partial | Tool-client message model, synchronized registry, broker, runtime, security policy checks, idempotency cache, audit events, async broker pending/timeout/cancel handling, runtime-side cooperative cancellation tokens, synchronized in-memory paired transport, and injected realtime server/client transport semantics with synchronized clientId binding and disconnect propagation are ported. Production network bindings remain external/injected under the zero-dependency constraint. |
| `observability`, `observability-memory`, `observability-console`, `observability-opentelemetry` | `observability`, `execution` | Partial | Structured log, metric, and trace records, fan-out pipeline with Node-style config-object construction, thread-safe adapter snapshots, adapter error isolation, synchronized in-memory collector and memory pipeline config objects with snapshot accessors, console JSON adapters, and injected zero-dependency OpenTelemetry bridge config objects are ported. Native OpenTelemetry SDK exporters and production sink configuration remain. |
| `autonomous`, `autonomous-postgres`, `approval-postgres` | `autonomous`, `runtime`, `execution`, `server` | Partial | Autonomous run/step/event/checkpoint model, synchronized in-memory store, synchronized file-backed store, injected zero-dependency Postgres autonomous-store SQL semantics with synchronized readiness/close lifecycle, static planner, AgentRunner JSON planner adapter, callback step executor, AgentRunner step executor, run manager, dependency scheduling, waiting/resume, cancel, checkpoints, events, run filters, JSON/result serialization, core server autonomous routes with Node-style create/lifecycle statuses, strict optional-field validation, and owner/tenant access scopes, in-memory/file approval stores with server routes, and injected zero-dependency Postgres approval-store SQL semantics with synchronized readiness/close lifecycle are ported. Full server parity and production Postgres client implementations remain external/injected under the zero-dependency constraint. |

Current verification:

```bash
cmake --build build
ctest --test-dir build --output-on-failure
./agent_native_smoke  # repeated 10 consecutive times
```

The current native build also passes the zero-dependency dependency scans used during
porting audits; the optional llama.cpp runtime remains disabled by default and the
native stub is linked in that configuration.
